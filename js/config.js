/* ============================================================
   ⚙️ YOUR SETTINGS — edit this file only
   ============================================================ */
window.VG_CONFIG = {
  /* Website admin login (gate for the admin page only).
     Donor data is NEVER kept in the browser — it lives in
     data/donors.xlsx on your disk. */
  ADMIN_USER: 'admin',
  ADMIN_PASS: 'VNG@2026'
};
